{
  "name": "amanah",
  "version": "1.0.0",
  "private": true,
  "description": "Amanah — autonomous treasury agent for charities and NGOs (Binance Agent OS Mini Hackathon)",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "demo": "pnpm --filter agent run demo",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
